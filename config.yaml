# 摄像头配置
cameras:
  - name: "客厅摄像头"
    rtsp_url: "rtsp://zhanghao:mima@172.154.200.150:554/h264/ch1/main/av_stream"

# 录制配置
recording:
  segment_duration: 60  # 视频片段时长（秒）
  video_root: "/recordings"  # 视频存储根目录
  retention_days: 7  # 保留天数

# 清理配置
cleanup:
  interval_hours: 6  # 清理间隔（小时）
